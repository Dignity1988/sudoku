
*/
function creatingrandom(){
for (i=1;i=9;i++){
arr[]=['1'].push(['1']);
}
/*immidiately outpooting fuction*/
document.write(arr[]);
}
































/*





var arr = [1, 2, 3, 4, 5];
function matrix(m, n) {
      var result = [];
      for(var i = 0; i < n; i++)
      {
      for (var k = 0; k < n; k++)
      {
      for(var j = 0; j < n; j++) {
          result.push(new Array(m,n).fill(i,k,j));
      }
      }
      }
      return result;
  }

matrix.prototype.multiply = function(m.n){

}

var arr = [1, 2, 3, 4, 5];

function compareRandom() {
  return Math.floor(Math.random() - 0.5);
}
//create unique array of numbers from 1 to 9
arr.sort(compareRandom);








arr.sort(compareRandom);


alert( arr ) // элементы в случайном порядке, например [3,5,1,2,4]




  function matrix(m, n) {

        for(var j = 0; j < n; j++) {
            result.push(new Array(m,n).fill(i,k,j));
        }

        return result;
    }

matrix.prototype.multiply = function(m.n){

}
/*
To create a multi dimensional array you can simply push arrays to one array.

var a = [1,2,3];
var b = [4,5,6];
var c = [];
c.push(a);
c.push(b);
console.log(c); // [[1, 2, 3], [4, 5, 6]]

*/



























/*

//sorting randomzid variation of matrix type

function sortoutput
var names = ['HTML', 'CSS', 'JavaScript'];

var nameLengths = names.map(function(name) {
return name.length; - ordering fucntion should be applied

});

// получили массив с длинами
alert( nameLengths ); // 4,3,10

//create 9 equialent matrix that are not identical and place them in . $output . cosequence one after another











};
*/
